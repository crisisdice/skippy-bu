{
  "name": "api",
  "version": "1.0.0",
  "description": "REST API app with cards.",
  "private": true,
  "scripts": {
    "test": "(cd ./client && yarn build && yarn start)"
  },
  "author": "Alexander Daily",
  "workspaces": {
    "packages": [
      "./client/",
      "./engine/",
      "./crud-service/",
      "./logic-gateway/"
    ]
  },
  "resolutions": {
    "**/@prisma/client": "3.7.0",
    "**/prisma": "3.7.0",
    "**/typescript": "4.5.4"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "rxjs": "^7.5.2"
  }
}
