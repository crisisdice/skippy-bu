import {
  Action
} from 'skip-models'

import {
  listQuestion,
  spinner,
} from '../elements'

import {
  GameClient,
} from '../clients'

import {
  t,
} from '../i8n'

export async function game(client: GameClient) {
  return
  //while (true) {
  //}
}

